package com.webhunt.app.ui.theme

import androidx.compose.ui.graphics.Color

// WebHunt Monochromatic Pure Dark Tokens
val WebHuntBlack = Color(0xFF000000)          // Deep background
val WebHuntCard = Color(0xFF0D0D0D)           // Primary card background
val WebHuntSurface = Color(0xFF080808)        // Inner inputs, search fields
val WebHuntHover = Color(0xFF161616)          // Elevated pill & hover surface
val WebHuntBorder = Color(0x1FF8F3F0)         // Hairline cream border (rgba(248, 243, 240, 0.12))
val WebHuntBorderSubtle = Color(0x0FF8F3F0)   // Very subtle divider (rgba(248, 243, 240, 0.06))

// Brand Accents
val WebHuntRoyal = Color(0xFF0048BB)          // Primary Royal Blue
val WebHuntRoyalHover = Color(0xFF00388A)     // Pressed Royal Blue
val WebHuntRoyalTint = Color(0x330048BB)      // Royal Blue with 20% alpha

// Typography
val WebHuntPaper = Color(0xFFF8F3F0)          // Primary typography (Cream paper)
val WebHuntMuted = Color(0xFFA8A196)          // Muted stone subtext

// Semantic Status
val WebHuntEmerald = Color(0xFF5EBA8C)        // Phone & verified status
val WebHuntEmeraldTint = Color(0x265EBA8C)    // Emerald with 15% alpha
val WebHuntRed = Color(0xFFEF4444)            // Error text & notice
val WebHuntRedDark = Color(0xFF261010)        // Error notice background
val WebHuntGold = Color(0xFFFBBF24)           // Star ratings
val WebHuntBlueBadge = Color(0xFF10192A)      // Notice banner background
